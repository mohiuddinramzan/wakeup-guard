{
  "name": "wakeup-guard",
  "version": "1.0.0",
  "description": "Wakeup Guard — an alarm app you actually have to earn the right to dismiss.",
  "scripts": {
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
